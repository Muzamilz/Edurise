# Authentication & Role-Based Routing Flow Diagram

## 🔄 Complete Authentication Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER LOGIN PROCESS                          │
└─────────────────────────────────────────────────────────────────────┘

    ┌──────────────┐
    │   User at    │
    │ Login Page   │
    └──────┬───────┘
           │
           │ Enters email & password
           ↓
    ┌──────────────┐
    │  LoginForm   │
    │  Component   │
    └──────┬───────┘
           │
           │ calls useAuth().login()
           ↓
    ┌──────────────┐
    │  Auth Store  │
    │   (Pinia)    │
    └──────┬───────┘
           │
           │ POST /api/accounts/auth/login/
           ↓
    ┌──────────────┐
    │   Backend    │
    │  LoginView   │
    └──────┬───────┘
           │
           │ Validates credentials
           ↓
    ┌──────────────┐
    │ JWTAuthService│
    │.generate_tokens()│
    └──────┬───────┘
           │
           │ Creates JWT with role info
           ↓
    ┌──────────────────────────────────────┐
    │         JWT Token Structure          │
    ├──────────────────────────────────────┤
    │ • user_id                            │
    │ • email                              │
    │ • role: "superuser|admin|teacher|    │
    │         student"                     │
    │ • is_teacher                         │
    │ • is_approved_teacher                │
    │ • is_staff                           │
    │ • is_superuser                       │
    │ • tenant_id                          │
    │ • tenant_role                        │
    └──────────────┬───────────────────────┘
                   │
                   │ Returns tokens + user data
                   ↓
            ┌──────────────┐
            │  Auth Store  │
            │ Stores Data  │
            └──────┬───────┘
                   │
                   │ Computes userRole
                   ↓
            ┌──────────────────────────────┐
            │    Role Determination        │
            ├──────────────────────────────┤
            │ if is_superuser → superuser  │
            │ if is_staff → admin          │
            │ if is_approved_teacher →     │
            │    teacher                   │
            │ if is_teacher → teacher-     │
            │    pending                   │
            │ else → student               │
            └──────┬───────────────────────┘
                   │
                   │ Computes dashboardRoute
                   ↓
            ┌──────────────────────────────┐
            │   Dashboard Route Mapping    │
            ├──────────────────────────────┤
            │ superuser → /super-admin/    │
            │             organizations    │
            │ admin → /admin/users         │
            │ teacher → /teacher/courses   │
            │ teacher-pending →            │
            │   /teacher/application-status│
            │ student → /dashboard         │
            └──────┬───────────────────────┘
                   │
                   │ router.push(dashboardRoute)
                   ↓
            ┌──────────────┐
            │   Vue Router │
            └──────┬───────┘
                   │
                   │ Navigates to route
                   ↓
            ┌──────────────┐
            │ Route Guard  │
            │   (authGuard)│
            └──────┬───────┘
                   │
                   │ Checks authentication
                   ↓
            ┌──────────────┐
            │  Dashboard   │
            │  Component   │
            └──────┬───────┘
                   │
                   │ Renders role-specific dashboard
                   ↓
    ┌──────────────────────────────────────┐
    │     Role-Specific Dashboard          │
    ├──────────────────────────────────────┤
    │ • SuperAdminDashboard                │
    │ • AdminDashboard                     │
    │ • TeacherDashboard                   │
    │ • StudentDashboard                   │
    └──────────────────────────────────────┘
```

## 🎭 Role Hierarchy & Access

```
┌─────────────────────────────────────────────────────────────┐
│                    ROLE HIERARCHY                           │
└─────────────────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────┐
    │         🔴 SUPERUSER                    │
    │  Platform-wide Administrator            │
    │  • Manage all organizations             │
    │  • View all users & courses             │
    │  • System configuration                 │
    │  • Global analytics                     │
    └─────────────────┬───────────────────────┘
                      │ Inherits all below
                      ↓
    ┌─────────────────────────────────────────┐
    │         🟠 ADMIN                        │
    │  Organization Administrator             │
    │  • Manage organization users            │
    │  • Approve teachers                     │
    │  • Manage courses                       │
    │  • Organization analytics               │
    └─────────────────┬───────────────────────┘
                      │ Inherits all below
                      ↓
    ┌─────────────────────────────────────────┐
    │         🟢 TEACHER (Approved)           │
    │  Course Creator & Instructor            │
    │  • Create & manage courses              │
    │  • View enrolled students               │
    │  • Conduct live classes                 │
    │  • Track student progress               │
    └─────────────────┬───────────────────────┘
                      │ Inherits all below
                      ↓
    ┌─────────────────────────────────────────┐
    │         🟡 TEACHER (Pending)            │
    │  Teacher Awaiting Approval              │
    │  • View application status              │
    │  • Access student features              │
    │  • Cannot create courses yet            │
    └─────────────────┬───────────────────────┘
                      │ Inherits all below
                      ↓
    ┌─────────────────────────────────────────┐
    │         🔵 STUDENT                      │
    │  Learner                                │
    │  • Enroll in courses                    │
    │  • Attend live classes                  │
    │  • Track own progress                   │
    │  • View certificates                    │
    └─────────────────────────────────────────┘
```

## 🛣️ Route Protection Flow

```
┌─────────────────────────────────────────────────────────────┐
│                  ROUTE PROTECTION                           │
└─────────────────────────────────────────────────────────────┘

User Navigates to Route
         │
         ↓
    ┌─────────────┐
    │ Route Guard │
    │   Checks    │
    └──────┬──────┘
           │
           ├─→ No Auth? ──→ Redirect to /auth/login
           │
           ├─→ Guest Route + Authenticated? ──→ Redirect to dashboardRoute
           │
           ├─→ Requires Teacher + Not Teacher? ──→ Redirect to /unauthorized
           │
           ├─→ Requires Admin + Not Admin? ──→ Redirect to /unauthorized
           │
           ├─→ Requires Superuser + Not Superuser? ──→ Redirect to /unauthorized
           │
           └─→ All Checks Pass ──→ Allow Access
                                        │
                                        ↓
                                 Render Component
```

## 🔐 Token Lifecycle

```
┌─────────────────────────────────────────────────────────────┐
│                    TOKEN LIFECYCLE                          │
└─────────────────────────────────────────────────────────────┘

    Login
      │
      ↓
    Generate JWT
    (15 min expiry)
      │
      ├─→ Store in localStorage
      │
      └─→ Include in API requests
            │
            ↓
    ┌──────────────┐
    │ Token Valid? │
    └──────┬───────┘
           │
           ├─→ Yes ──→ Process Request
           │
           └─→ No (Expired)
                 │
                 ↓
           Use Refresh Token
           (7 days expiry)
                 │
                 ├─→ Valid ──→ Generate New Access Token
                 │
                 └─→ Invalid ──→ Logout & Redirect to Login
```

## 🎯 Dashboard Component Selection

```
┌─────────────────────────────────────────────────────────────┐
│              DASHBOARD COMPONENT LOGIC                      │
└─────────────────────────────────────────────────────────────┘

DashboardView.vue
      │
      ↓
Check authStore.userRole
      │
      ├─→ 'superuser' ──→ <SuperAdminDashboard />
      │                    └─→ /super-admin/organizations
      │
      ├─→ 'admin' ──→ <AdminDashboard />
      │                └─→ /admin/users
      │
      ├─→ 'teacher' ──→ <TeacherDashboard />
      │                 └─→ /teacher/courses
      │
      ├─→ 'teacher-pending' ──→ <TeacherDashboard />
      │                          └─→ /teacher/application-status
      │
      └─→ 'student' ──→ <StudentDashboard />
                        └─→ /dashboard
```

## 🔄 Multi-Tenant Flow

```
┌─────────────────────────────────────────────────────────────┐
│                  MULTI-TENANT SUPPORT                       │
└─────────────────────────────────────────────────────────────┘

User Belongs to Multiple Organizations
              │
              ↓
    ┌──────────────────┐
    │ Select Tenant    │
    │ (Organization)   │
    └────────┬─────────┘
             │
             │ switchTenant(tenantId)
             ↓
    ┌──────────────────┐
    │ Backend Validates│
    │ User Access      │
    └────────┬─────────┘
             │
             │ Generate New JWT with Tenant Context
             ↓
    ┌──────────────────┐
    │ Update Tokens    │
    │ & Tenant Info    │
    └────────┬─────────┘
             │
             │ Reload Page with New Context
             ↓
    ┌──────────────────┐
    │ Show Tenant-     │
    │ Specific Data    │
    └──────────────────┘
```

## 📊 Data Flow Summary

```
┌─────────────────────────────────────────────────────────────┐
│                    DATA FLOW                                │
└─────────────────────────────────────────────────────────────┘

Frontend                Backend                 Database
   │                       │                        │
   │ Login Request         │                        │
   ├──────────────────────→│                        │
   │                       │ Validate User          │
   │                       ├───────────────────────→│
   │                       │                        │
   │                       │ User Data              │
   │                       │←───────────────────────┤
   │                       │                        │
   │                       │ Get UserProfile        │
   │                       ├───────────────────────→│
   │                       │                        │
   │                       │ Profile Data           │
   │                       │←───────────────────────┤
   │                       │                        │
   │                       │ Generate JWT           │
   │                       │ (with role info)       │
   │                       │                        │
   │ JWT + User Data       │                        │
   │←──────────────────────┤                        │
   │                       │                        │
   │ Store in State        │                        │
   │ & localStorage        │                        │
   │                       │                        │
   │ Compute Role          │                        │
   │ & Dashboard Route     │                        │
   │                       │                        │
   │ Navigate to           │                        │
   │ Dashboard             │                        │
   │                       │                        │
```

## 🎨 Visual Role Indicators

```
┌─────────────────────────────────────────────────────────────┐
│                  ROLE VISUAL INDICATORS                     │
└─────────────────────────────────────────────────────────────┘

🔴 Superuser    → Red Badge    → Full Platform Access
🟠 Admin        → Orange Badge → Organization Management
🟢 Teacher      → Green Badge  → Course Creation
🟡 Pending      → Yellow Badge → Limited Access
🔵 Student      → Blue Badge   → Learning Access
```

## 📝 Quick Reference

```
┌─────────────────────────────────────────────────────────────┐
│                    QUICK REFERENCE                          │
└─────────────────────────────────────────────────────────────┘

Role Detection:
  authStore.userRole → 'superuser' | 'admin' | 'teacher' | 'student'

Dashboard Route:
  authStore.dashboardRoute → Role-specific route

Navigation:
  useRoleBasedRouting().goToDashboard()

Role Check:
  useRoleBasedRouting().hasRole('teacher')

Access Check:
  useRoleBasedRouting().canAccessRoute(routeMeta)
```

---

**Note**: This diagram represents the complete authentication and role-based routing flow. Each component is designed to work together seamlessly to provide secure, role-appropriate access to the platform.
